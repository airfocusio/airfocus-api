---
layout: default
nav_exclude: true
search_exclude: true
---

## 1.0.0-beta.34.16.3 (2025-05-15)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api tag `Views:Inbox` added

## 1.0.0-beta.34.16.2 (2025-05-06)

No changes to report, but the specs are different

## 1.0.0-beta.34.16.1 (2025-05-04)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api tag `Views:Inbox` removed

## 1.0.0-beta.34.15.1 (2025-04-10)

No changes to report, but the specs are different

## 1.0.0-beta.34.14.4 (2025-04-02)

No changes to report, but the specs are different

## 1.0.0-beta.34.14.2 (2025-04-01)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api tag `Views:Inbox` added

## 1.0.0-beta.34.13.0 (2025-03-27)

No changes to report, but the specs are different

## 1.0.0-beta.34.9.1 (2025-03-25)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api tag `Views:Inbox` removed

## 1.0.0-beta.34.9.0 (2025-03-24)

No changes to report, but the specs are different

## 1.0.0-beta.34.8.0 (2025-03-23)

No changes to report, but the specs are different

## 1.0.0-beta.34.7.6 (2025-03-22)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api tag `Views:Inbox` added

## 1.0.0-beta.34.7.5 (2025-03-20)

No changes to report, but the specs are different

## 1.0.0-beta.34.7.3 (2025-03-17)

No changes to report, but the specs are different

## 1.0.0-beta.34.7.0 (2025-03-12)

No changes to report, but the specs are different

## 1.0.0-beta.34.6.9 (2025-03-11)

No changes to report, but the specs are different

## 1.0.0-beta.34.6.8 (2025-03-09)


### POST /api/workspaces/{workspaceId}/milestones
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `workspaceId`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/milestones/bulk
- ⚠️ removed the request property `items/oneOf[subschema #1: MilestoneBulkCreate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: MilestoneBulkCreate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #1: MilestoneBulkCreate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: MilestoneBulkCreate]/resource/workspaceId`
- ⚠️ removed the request property `items/oneOf[subschema #4: MilestoneBulkUpdate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: MilestoneBulkUpdate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #4: MilestoneBulkUpdate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: MilestoneBulkUpdate]/resource/workspaceId`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/milestones/list
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/milestones/search
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}/milestones/{milestoneId}
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `workspaceId`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`




-  removed the schema `Milestone`

## 1.0.0-beta.34.6.5 (2025-03-07)


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  api operation id `retrieveInboxCounters` removed and replaced with `retrieveInboxViewCounters`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### POST /api/workspaces/{workspaceId}/links/bulk
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### POST /api/workspaces/{workspaceId}/links/list
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### POST /api/workspaces/{workspaceId}/links/search
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### DELETE /api/workspaces/{workspaceId}/links/{itemLinkId}
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### GET /api/workspaces/{workspaceId}/links/{itemLinkId}
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### PATCH /api/workspaces/{workspaceId}/links/{itemLinkId}
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed


### PUT /api/workspaces/{workspaceId}/links/{itemLinkId}
-  api tag `Items:Links` added
-  api tag `ItemLinks` removed




-  removed the schema `Comment`
-  removed the schema `CommentEmbed`
-  removed the schema `CommentReaction`
-  removed the schema `CommentReactionEmoji`
-  removed the schema `CommentSearchQuery`
-  removed the schema `CommentWithCommentEmbed`
-  removed the schema `CommentWithCommentEmbedOrNullList`
-  removed the schema `CommentWithCommentEmbedPage`
-  removed the schema `InboxCounter`
-  removed the schema `InboxViewEndpointsInboxCountersRequest`
-  removed the schema `LinkConstraint`

## 1.0.0-beta.34.5.2 (2025-03-03)

No changes to report, but the specs are different

## 1.0.0-beta.34.5.1 (2025-02-28)


### GET /api/profile
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/profile
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/profile/api-keys
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/profile/api-keys
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/profile/api-keys/{apiKeyId}
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/profile/change-password
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/profile/client-settings
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/profile/client-settings
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/team
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/team/flags
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/tos
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/team/users
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/disabled
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/bulk
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/create
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/resend
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/kick
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/role
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/templates
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/templates/{templateId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the request property `archived`
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `legacyId`
- ⚠️ removed the request property `metadata`
- ⚠️ removed the request property `namespace`
- ⚠️ removed the request property `teamId`
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  the request property `progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/bulk
- ⚠️ removed the required property `items/_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/archived` from the response with the `200` status
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/archived`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/legacyId`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/metadata`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/namespace`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/teamId`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/archived`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/legacyId`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/metadata`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/namespace`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/teamId`
- ⚠️ removed the optional property `items/_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/legacyId` from the response with the `200` status
-  the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/progressMode` became optional
-  the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/count
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/events/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/global-search
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/groups
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/bulk
- ⚠️ removed the required property `items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/list
- ⚠️ removed the required property `items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/search
- ⚠️ removed the required property `items/items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### GET /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### PATCH /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### PUT /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### GET /api/workspaces/items/alias/{aliasValue}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/copy
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/list
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  api operation id `retrieveItemsGlobally` removed and replaced with `listItemsGlobal`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/move
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/list
- ⚠️ removed the required property `items/_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/search
- ⚠️ removed the required property `items/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/items/legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/templates
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/templates/{templateId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/workspace-relations/set
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status


### DELETE /api/workspaces/{workspaceId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the request property `archived`
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `legacyId`
- ⚠️ removed the request property `metadata`
- ⚠️ removed the request property `namespace`
- ⚠️ removed the request property `teamId`
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  the request property `progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/background-activity/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/{workspaceId}/duplicate
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/events/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/{workspaceId}/items
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json)
- ⚠️ removed the request property `description/blocks` (media type: application/json+markdown)
- ⚠️ removed the request property `id` (media type: application/json)
- ⚠️ removed the request property `id` (media type: application/json+markdown)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `number` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json+markdown)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `workspaceId` (media type: application/json+markdown)
- ⚠️ removed the request property `workspaceId` (media type: application/json)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json)
-  the request property `archived` became optional (media type: application/json+markdown)
-  the `archived` request property default value `false` was added (media type: application/json+markdown)
-  the `archived` request property default value `false` was added (media type: application/json)
-  the `description` request property type/format was generalized from `object`/`` to `string`/`` (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/bulk
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/list
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/reset-colors
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/search
- ⚠️ the `items/items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/unwatch
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/watch
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/workspaces/{workspaceId}/items/{itemId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json)
- ⚠️ removed the request property `createdAt` (media type: application/json+markdown)
- ⚠️ removed the request property `description/blocks` (media type: application/json+markdown)
- ⚠️ removed the request property `id` (media type: application/json)
- ⚠️ removed the request property `id` (media type: application/json+markdown)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `number` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json+markdown)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `workspaceId` (media type: application/json)
- ⚠️ removed the request property `workspaceId` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json)
-  the request property `archived` became optional (media type: application/json+markdown)
-  the `archived` request property default value `false` was added (media type: application/json+markdown)
-  the `archived` request property default value `false` was added (media type: application/json)
-  the `description` request property type/format was generalized from `object`/`` to `string`/`` (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links
- ⚠️ removed the request property `id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/bulk
- ⚠️ removed the request property `items/oneOf[subschema #1: ItemLinkBulkCreate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #4: ItemLinkBulkUpdate]/resource/id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/list
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/search
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}/links/{itemLinkId}
- ⚠️ removed the request property `id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/permissions
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/statuses
- ⚠️ added the new required request property `statuses/items/category`
- ⚠️ added the new required request property `statuses/items/default`
- ⚠️ added the new required request property `statuses/items/id`
- ⚠️ added the new required request property `statuses/items/name`
- ⚠️ added the new required request property `statuses/items/order`
- ⚠️ added the new required request property `statuses/items/workspaceId`
- ⚠️ removed `#/components/schemas/CreateStatus, #/components/schemas/UpdateStatus` from the `statuses/items/` request property `oneOf` list
- ⚠️ the `statuses/items/` request property type/format changed from ``/`` to `object`/``
-  added the new optional request property `statuses/items/color`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`




-  removed the schema `CreateStatus`
-  removed the schema `ItemLink`
-  removed the schema `UpdateStatus`
-  removed the schema `WorkspaceServerEndpointsSetStatusesRequest`
-  removed the schema `WorkspaceServerEndpointsWorkspaceDuplicateRequest`
-  removed the schema `WorkspaceServerEndpointsWorkspacePermissionsUpdateRequest`

## 1.0.0-beta.34.5.0 (2025-02-28)


### GET /api/profile
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/profile
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/profile/api-keys
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/profile/api-keys
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/profile/api-keys/{apiKeyId}
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/profile/change-password
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/profile/client-settings
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/profile/client-settings
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/team
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/team/flags
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/tos
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/team/users
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/disabled
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/bulk
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/create
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/invite/resend
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/kick
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/team/users/role
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/templates
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/templates/{templateId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the request property `archived`
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `legacyId`
- ⚠️ removed the request property `metadata`
- ⚠️ removed the request property `namespace`
- ⚠️ removed the request property `teamId`
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  the request property `progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/bulk
- ⚠️ removed the required property `items/_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/archived` from the response with the `200` status
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/archived`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/legacyId`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/metadata`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/namespace`
- ⚠️ removed the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/teamId`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/archived`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/createdAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/lastUpdatedAt`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/legacyId`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/metadata`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/namespace`
- ⚠️ removed the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/teamId`
- ⚠️ removed the optional property `items/_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/legacyId` from the response with the `200` status
-  the request property `items/oneOf[subschema #1: WorkspaceBulkCreate]/resource/progressMode` became optional
-  the request property `items/oneOf[subschema #4: WorkspaceBulkUpdate]/resource/progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/count
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/events/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/global-search
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/groups
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/bulk
- ⚠️ removed the required property `items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/list
- ⚠️ removed the required property `items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### POST /api/workspaces/groups/search
- ⚠️ removed the required property `items/items/_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/items/_embedded/workspaces/items/legacyId` from the response with the `200` status


### GET /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### PATCH /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### PUT /api/workspaces/groups/{workspaceGroupId}
- ⚠️ removed the required property `_embedded/workspaces/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/workspaces/items/legacyId` from the response with the `200` status


### GET /api/workspaces/items/alias/{aliasValue}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/copy
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/list
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  api operation id `retrieveItemsGlobally` removed and replaced with `listItemsGlobal`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/items/move
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/list
- ⚠️ removed the required property `items/_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `items/legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/search
- ⚠️ for the `query` request parameter `limit`, default value was changed from `1000.00` to `50.00`
- ⚠️ removed the required property `items/items/archived` from the response with the `200` status
- ⚠️ removed the optional property `items/items/legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/templates
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/templates/{templateId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/workspace-relations/set
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status


### DELETE /api/workspaces/{workspaceId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the request property `archived`
- ⚠️ removed the request property `createdAt`
- ⚠️ removed the request property `id`
- ⚠️ removed the request property `lastUpdatedAt`
- ⚠️ removed the request property `legacyId`
- ⚠️ removed the request property `metadata`
- ⚠️ removed the request property `namespace`
- ⚠️ removed the request property `teamId`
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  the request property `progressMode` became optional
-  the `progressMode` request property default value `count` was added
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/background-activity/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/{workspaceId}/duplicate
- ⚠️ removed the required property `_embedded/children/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `_embedded/parents/items/workspace/archived` from the response with the `200` status
- ⚠️ removed the required property `archived` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/children/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `_embedded/parents/items/workspace/legacyId` from the response with the `200` status
- ⚠️ removed the optional property `legacyId` from the response with the `200` status
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/events/sse
- ⚠️ api path removed without deprecation


### POST /api/workspaces/{workspaceId}/items
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json)
- ⚠️ removed the request property `description/blocks` (media type: application/json+markdown)
- ⚠️ removed the request property `id` (media type: application/json)
- ⚠️ removed the request property `id` (media type: application/json+markdown)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json+markdown)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `workspaceId` (media type: application/json+markdown)
- ⚠️ removed the request property `workspaceId` (media type: application/json)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json)
-  the `archived` request property default value `false` was added (media type: application/json)
-  the `archived` request property default value `false` was added (media type: application/json+markdown)
-  the `description` request property type/format was generalized from `object`/`` to `string`/`` (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/bulk
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/list
- ⚠️ the `items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/reset-colors
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/search
- ⚠️ for the `query` request parameter `limit`, default value was changed from `1000.00` to `50.00`
- ⚠️ the `items/items/description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `items/items/description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/unwatch
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/items/watch
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/workspaces/{workspaceId}/items/{itemId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}/items/{itemId}
- ⚠️ the `description` response's property type/format changed from `object`/`` to `string`/`` for status `200` (media type: application/json+markdown)
- ⚠️ removed the request property `createdAt` (media type: application/json)
- ⚠️ removed the request property `createdAt` (media type: application/json+markdown)
- ⚠️ removed the request property `description/blocks` (media type: application/json+markdown)
- ⚠️ removed the request property `id` (media type: application/json+markdown)
- ⚠️ removed the request property `id` (media type: application/json)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `lastUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json)
- ⚠️ removed the request property `number` (media type: application/json+markdown)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `statusCategoryUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json+markdown)
- ⚠️ removed the request property `statusUpdatedAt` (media type: application/json)
- ⚠️ removed the request property `workspaceId` (media type: application/json+markdown)
- ⚠️ removed the request property `workspaceId` (media type: application/json)
- ⚠️ removed the optional property `description/blocks` from the response with the `200` status (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json+markdown)
-  the request property `archived` became optional (media type: application/json)
-  the `archived` request property default value `false` was added (media type: application/json)
-  the `archived` request property default value `false` was added (media type: application/json+markdown)
-  the `description` request property type/format was generalized from `object`/`` to `string`/`` (media type: application/json+markdown)
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links
- ⚠️ removed the request property `id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/bulk
- ⚠️ removed the request property `items/oneOf[subschema #1: ItemLinkBulkCreate]/resource/id`
- ⚠️ removed the request property `items/oneOf[subschema #4: ItemLinkBulkUpdate]/resource/id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/list
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/links/search
- ⚠️ for the `query` request parameter `limit`, default value was changed from `1000.00` to `50.00`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### DELETE /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### GET /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PATCH /api/workspaces/{workspaceId}/links/{itemLinkId}
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### PUT /api/workspaces/{workspaceId}/links/{itemLinkId}
- ⚠️ removed the request property `id`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/permissions
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`


### POST /api/workspaces/{workspaceId}/statuses
- ⚠️ added the new required request property `statuses/items/category`
- ⚠️ added the new required request property `statuses/items/default`
- ⚠️ added the new required request property `statuses/items/id`
- ⚠️ added the new required request property `statuses/items/name`
- ⚠️ added the new required request property `statuses/items/order`
- ⚠️ added the new required request property `statuses/items/workspaceId`
- ⚠️ removed `#/components/schemas/CreateStatus, #/components/schemas/UpdateStatus` from the `statuses/items/` request property `oneOf` list
- ⚠️ the `statuses/items/` request property type/format changed from ``/`` to `object`/``
-  added the new optional request property `statuses/items/color`
-  added the non-success response with the status `400`
-  added the non-success response with the status `401`
-  added the non-success response with the status `403`




-  removed the schema `CreateStatus`
-  removed the schema `ItemLink`
-  removed the schema `UpdateStatus`
-  removed the schema `WorkspaceServerEndpointsSetStatusesRequest`
-  removed the schema `WorkspaceServerEndpointsWorkspaceDuplicateRequest`
-  removed the schema `WorkspaceServerEndpointsWorkspacePermissionsUpdateRequest`

## 1.0.0-beta.34.4.9 (2025-02-28)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.8 (2025-02-27)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.7 (2025-02-26)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.4 (2025-02-22)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.3 (2025-02-21)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.1 (2025-02-17)

No changes to report, but the specs are different

## 1.0.0-beta.34.4.0 (2025-02-15)

No changes to report, but the specs are different

## 1.0.0-beta.34.3.1 (2025-02-14)

No changes to report, but the specs are different

## 1.0.0-beta.34.2.1 (2025-02-12)

No changes to report, but the specs are different

## 1.0.0-beta.34.0.0 (2025-02-10)


### POST /api/fields
-  deleted the `query` request parameter `token` with deprecation


### POST /api/fields/reorder
-  deleted the `query` request parameter `token` with deprecation


### POST /api/fields/search
-  deleted the `query` request parameter `token` with deprecation


### GET /api/fields/types
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/fields/workspaces
-  deleted the `query` request parameter `token` with deprecation


### POST /api/fields/workspaces
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/fields/{fieldId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/fields/{fieldId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/fields/{fieldId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/profile
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/profile
-  deleted the `query` request parameter `token` with deprecation


### GET /api/profile/api-keys
-  deleted the `query` request parameter `token` with deprecation


### POST /api/profile/api-keys
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/profile/api-keys/{apiKeyId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/profile/change-password
-  deleted the `query` request parameter `token` with deprecation


### GET /api/profile/client-settings
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/profile/client-settings
-  deleted the `query` request parameter `token` with deprecation


### GET /api/team
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/team/flags
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/tos
-  deleted the `query` request parameter `token` with deprecation


### GET /api/team/users
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/disabled
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/invite
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/invite/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/invite/create
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/invite/resend
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/kick
-  deleted the `query` request parameter `token` with deprecation


### POST /api/team/users/role
-  deleted the `query` request parameter `token` with deprecation


### GET /api/templates
-  deleted the `query` request parameter `token` with deprecation


### POST /api/templates/{templateId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/bulk
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/count
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/events/sse
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/extensions/fields
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/extensions/views/inbox/{viewId}/counters
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/fields/add-team-field
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/fields/install-field
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/fields/reconfigure-field
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/fields/team-fields
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/global-search
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups/assign
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/groups/{workspaceGroupId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/groups/{workspaceGroupId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/groups/{workspaceGroupId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/groups/{workspaceGroupId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/groups/{workspaceGroupId}/permissions
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/item-relations
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/item-relations/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/item-relations/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/item-relations/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/item-relations/{itemRelationId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/item-relations/{itemRelationId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/item-relations/{itemRelationId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/item-relations/{itemRelationId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/items/alias/{aliasValue}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/items/copy
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/items/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/items/move
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/search
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/templates
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/templates/{templateId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/uninstall-field
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/workspace-relations
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/workspace-relations/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/workspace-relations/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/workspace-relations/search
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/workspace-relations/set
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/workspace-relations/{workspaceRelationId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/workspace-relations/{workspaceRelationId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/workspace-relations/{workspaceRelationId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/workspace-relations/{workspaceRelationId}
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/attachments
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/attachments/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/attachments/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/attachments/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/attachments/{itemAttachmentId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/attachments/{itemAttachmentId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}/attachments/{itemAttachmentId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}/attachments/{itemAttachmentId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/background-activity/sse
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/comments
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/comments/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/comments/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/comments/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/comments/{commentId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/comments/{commentId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}/comments/{commentId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}/comments/{commentId}
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/comments/{commentId}/reactions/{emojiCode}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/comments/{commentId}/reactions/{emojiCode}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/duplicate
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/events/sse
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/install-field
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/reset-colors
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/search
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/unwatch
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/items/watch
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/items/{itemId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/items/{itemId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}/items/{itemId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}/items/{itemId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/links
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/links/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/links/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/links/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/links/{itemLinkId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/links/{itemLinkId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}/links/{itemLinkId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}/links/{itemLinkId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/milestones
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/milestones/bulk
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/milestones/list
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/milestones/search
-  deleted the `query` request parameter `token` with deprecation


### DELETE /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  deleted the `query` request parameter `token` with deprecation


### GET /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  deleted the `query` request parameter `token` with deprecation


### PATCH /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  deleted the `query` request parameter `token` with deprecation


### PUT /api/workspaces/{workspaceId}/milestones/{milestoneId}
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/permissions
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/reconfigure-field
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/reorder-fields
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/statuses
-  deleted the `query` request parameter `token` with deprecation


### POST /api/workspaces/{workspaceId}/uninstall-field
-  deleted the `query` request parameter `token` with deprecation

